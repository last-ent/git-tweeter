../Secrets expects a file called `secrets.txt` which has two CSV lines.
First line should be as shown between the `` while second one will be individual for each user.

* `consumerKey,consumerSecret,accessToken,accessSecret`
* Actual Keys for Twitter Authentication